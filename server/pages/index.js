import Head from "next/head";

const Home = () => (
  <div className="container">
    <Head>
      <title>Survey app server</title>
      <link rel="icon" href="/favicon.ico" />
    </Head>

    <main>
      <h1 className="title">Welcome!</h1>
    </main>
  </div>
);

export default Home;
